# Backend API pack guidance

For service and API work. GitHub backs PR wrap-up (parallel to base GitLab); the
database contract is **read-only** schema and query introspection for review —
never destructive DDL or arbitrary writes.

Teams may enable GitHub **or** GitLab for wrap-up via overlay bindings, not both
for the same role unless explicitly scoped.
